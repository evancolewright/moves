import { SafeAreaView, Text } from 'react-native';

const PartyDetailsScreen = ({ navigation }) => {
  return (
    <SafeAreaView>
      <Text> Party Details Screen </Text>
    </SafeAreaView>
  );
};

export default PartyDetailsScreen;
