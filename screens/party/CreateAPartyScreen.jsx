import { SafeAreaView, Text } from 'react-native';

const CreateAPartyScreen = ({ navigation }) => {
  return (
    <SafeAreaView>
      <Text> Create a Party Screen </Text>
    </SafeAreaView>
  );
};

export default CreateAPartyScreen;
